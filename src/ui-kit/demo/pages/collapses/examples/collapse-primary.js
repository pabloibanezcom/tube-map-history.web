const html = `<CollapseList
  color="primary"
  elements={lines}
  header={fakeHeader}
  content={fakeContent}
/>`;

const collapsePrimary = {
  id: 'primary',
  name: 'Primary',
  html
};

export default collapsePrimary;
