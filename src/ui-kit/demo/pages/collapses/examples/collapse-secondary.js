const html = `<CollapseList
  color="secondary"
  elements={lines}
  header={fakeHeader}
  content={fakeContent}
/>`;

const collapseSecondary = {
  id: 'secondary',
  name: 'Secondary',
  html
};

export default collapseSecondary;
